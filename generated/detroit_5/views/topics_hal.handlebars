{
	"_links": {
		"self": { "href": "{{ path }}" },
		{{#if parent}}"parent" : { "href" : "{{parent}}" },{{/if}}
		{{#if children}}
			{{#each children}}
				"{{this}}" : { "href" : "{{../path}}/{{this}}" },
			{{/each}}
		{{/if}}
		"curies": [{ "name": "topics", "href": "http://example.com/docs/rels/topics", "templated": false }]{{#if collection}},{{/if}}
		{{#if collection}}"next": { "href": "{{path}}?offset=50&limit=50" }{{/if}}
	},
	{{#if collection}}
	"_embedded" : {
		"topics" : [
			{{#each topics}}
				{
					"_links" : {
						"self": { "href": "{{path}}{{id}}" }
					},
"name" : "{{name}}",
"moderator" : "{{moderator}}",
"id" : "{{id}}"
				}{{#unless @last}},{{/unless}}
			{{/each}}
		]
	}
	{{else}}
"name" : "{{topics.name}}",
"moderator" : "{{topics.moderator}}",
"id" : "{{topics.id}}"
	{{/if}}
}