#!/bin/bash
echo Step 1: Initialize Git and Produce Tarball
cd {{location}}
#ls -l >> output.txt 2>&1

# npm install >> output.txt 2>&1
git init >> output.txt 2>&1
git add --all >> output.txt 2>&1
git commit -m "Initial commit of generated API || ModelShips)" >> output.txt 2>&1
git archive --format=tar.gz --output ../tarballs/{{filename}}.tar.gz master
wait
# git remote add origin {{githubUrl}}

echo Finished Step 1
echo Step 2: Launch App
# node server >> output.txt 2>&1