[
  {{#each model.title}}
    {{#unless @last }}{ "id" : "{{id}}", "length" : "{{length}}", "width" : "{{width}}", "height" : "{{height}}" },{{/unless}}
    {{#if @last }}{ "id" : "{{id}}", "length" : "{{length}}", "width" : "{{width}}", "height" : "{{height}}" }{{/if}}
  {{/each}}
] 